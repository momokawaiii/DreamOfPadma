# 构建矩阵

- 英文原文：[BuildMatrix.md](BuildMatrix.md)
- 状态：离线 Demo 验收目标；打包验收仍待完成
- 范围：[ChapterZero](../Rules/ChapterZero.zh-CN.md)

| 配置 | 用途 | 本 Demo 是否必需 |
|---|---|---|
| Editor Development / PIE | 原生编译、诊断与定向自动化 | 是，不能替代打包检查 |
| Win64 Development | 内部离线安装与完整路径调试 | 是 |
| Win64 Shipping | 离线发行质量安装与完整路径验收 | 是 |
| DebugGame / Test | 支持情况下的可选调试/自动化配置 | 不单独要求交付 |

## 两种包均需提供的证据

- 构建/Cook/打包日志标明构建版本、内容版本与配置。
- 安装或解压到干净测试位置，不依赖 Unreal Editor、项目源码路径、未 Cook 资产或 Steam 服务启动。安装器技术尚未选择。
- 启动/菜单/开场/第零章，经 Encounter、唯一 ACT 战斗、占领王庭到结尾可真实操作；这是正常路径，另行验证允许的教程跳过。
- 重新打开/继续、战斗失败/退出、教程跳过和主动重玩行为一致；同次重试不重复发奖。
- 烘焙地图元数据匹配 MapKey，无需运行时 PCG 重生成即可加载。这是目标验收，不是当前实现。
- PIE 外 UI/输入/焦点、资产引用、可读文本和干净退出正常。数值性能签收前需定义目标硬件/分辨率及性能预算。
- 最终黄金路径数值为获批生产值；公开发行资产按用户边界替换逆向/原型内容。
- 记录实际失败及剩余用户视觉/试玩验收；不能仅凭构建或自动化退出码宣称打包可玩已验证。

适用时使用已有 Scripts 入口和 Build/Profiles，执行前检查其当前能力。输出/证据放在 Artifacts，不写生成源码目录。TASK-053 没有执行打包。

Steam 接入/提交、在线测试、云存档和手柄认证要求不属于当前已接受范围。未来 Shipping 诊断/符号策略需显式配置，不能仅从配置名称推断。
